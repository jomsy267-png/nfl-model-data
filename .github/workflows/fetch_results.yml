name: nfl-pipeline

on:
  schedule:
    - cron: "0 16 * * *"   # 10:00 AM America/Edmonton ≈ 16:00 UTC
  workflow_dispatch:

jobs:
  sanity:
    runs-on: ubuntu-latest
    steps:
      - name: Echo
        run: echo "workflow loads"
